/**
 * Future structured visa destination records live here.
 * No country data, fees, or processing times until those facts are supplied.
 */

export type VisaDestination = {
  slug: string
  name: string
}
